## BASH and shell scripting and projects && notes

### Bash projects
My first project is composed of 4 bash files and 2 csv files one main entry(main.bash), to create a user (create-user.bash), to delete user (delete-user.bash), to list a  the user (see-all-user.bash). The 2 csv files serve as logs file
